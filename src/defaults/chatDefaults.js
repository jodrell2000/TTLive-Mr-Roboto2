// change the switch used to identify commands sent to your bot in your .env file
// copy the .env.example file to .env and fill out the details for your Bot user

import dotenv from 'dotenv';

export const commandIdentifier = process.env.COMMANDIDENTIFIER;